@page
@model WebAppNasaApi.Pages.MarsRoverModel
@using WebAppNasaApi.Pages.Components;
@using WebAppNasaApi.Pages.Shared


<div class="container-inputs p-3">
    <form method="post">
        <div class="row mb-3">
            <div class="col">
                <input type="date" class="form-control" id="date-earth" name="earth_date" min="2015-09-01" required/>
            </div>
            <div class="col">
                <select id="Rover-select" class="form-select" name="rover_name" aria-label="Default select example" required>
                    <option selected>--- Seleccione un Rover ---</option>
                    <option value="curiosity">Curioustity</option>
                    <option value="opportunity">Opportunity</option>
                    <option value="spirit">Spirit</option>
                </select>
            </div>
        </div>

        <div class="row">
            <div class="col">
                @*<button type="submit" class="btn btn-success" style="width:500px;">Get results!</button>*@
                <button type="submit" class="btn btn-success">Ver fotos</button>
                <a class="btn btn-info" data-bs-toggle="collapse" href="#collapseExample" role="button" aria-expanded="false" aria-controls="collapseExample">
                    Más información
                </a>
            </div>
            
        </div>
    </form>

    <div class="collapse" id="collapseExample">
        <component type="typeof(MoreInfoMars)" render-mode="ServerPrerendered" />
    </div>

</div>


@if (ViewData["IsPost"] is true)
{
    <div class="text-center">
        <div class="row row-cols-md-3 my-3">
            @foreach (var photo in Model._photosResponse.Photos)
            {
                <div class="col mb-3">
                    <div class="card">
                        <img src="@photo.img_src" class="card-img-top" alt="...">
                        <div class="card-body">
                            <p class="card-subtitle"> Camara: @photo.camera.full_name</p>
                            <p class="card-text"><small class="text-body-secondary">
                                    Fecha: @photo.earth_date.ToShortDateString()
                                </small>
                            </p>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
} 
else 
{
    @*<h1>Write some Date and select a rover from the select element and then, show me again!</h1>*@
    <h1></h1>
}